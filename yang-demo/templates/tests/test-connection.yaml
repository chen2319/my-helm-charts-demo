apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "yang-demo.fullname" . }}-test-connection"
  labels:
{{ include "yang-demo.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "yang-demo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
